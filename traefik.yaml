kind: Service
metadata:
  name: traefik
  namespace: traefik
spec:
  type: NodePort   # 👈 ADD THIS LINE
  selector:
    app: traefik
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # optional but recommended

